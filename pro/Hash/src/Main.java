public class Main {
    /**
     * 世界上有许多的哈希函数了，但是其一定满足下列特点：
     *
     * 哈希函数，可以认为
     * 输入域无穷，输出域有限制
     * （导致：不同的输入可能有相同的输出（哈希碰撞
     * 但是其发生概率非常低（是真的很低（如果你能认真举出例子去类比概率，那一定比你那个例子低
     * 具有均匀性，离散性的特点
     *
     * 输入相同数据一定返回相同结果
     * （哈希函数是不含有随机的
     * hashCode()方法可以用于生成输出
     *
     */

    public static void main(String[] args) {

        System.out.println("Hello world!");
    }
}